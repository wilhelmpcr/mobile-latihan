<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.WilhelmPaus" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/primary_desa</item>
        <item name="colorSecondary">@color/accent_desa</item>
        <item name="android:statusBarColor">@color/primary_dark_desa</item>
        <item name="colorOnPrimary">@color/white</item>
    </style>

    <style name="Theme.WilhelmPaus" parent="Base.Theme.WilhelmPaus" />
</resources>